For example, I am pretty sure all of you have run a `for` loop before.

It may have looked like this:

```python
for i in range(5):
    print("Hello, World")
```

But wait, have you realized this, though?

Let's look at this closely to understand what it's doing.
Manim animation: zoom closer to first.